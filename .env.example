# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your_key_here

# Shared login for beta testers
APP_USER=Mybooks
APP_PASS=quickscan123

# Vercel KV credentials (set via Vercel Storage integration)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Note: This key is now only used server-side (Vercel serverless function)
# It is NOT bundled into the client JavaScript
